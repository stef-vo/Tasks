/////////////////////////////////////
/////////////// test //////////
/////////////////////////////////////

/*Returns the number of items in the list.*/
int StringListSize(char*** list)
{
	int i = 0;
	while((*list)[i][0] != '\0')
		i++;
	return i;
}

/*Returns the number of items in the list.*/
int StringListSize(char*** list)
{
	int i = 0;
	while((*list)[i][0] != '\0')
		i++;
	return i;
}

/*Returns the number of items in the list.*/
int StringListSize(char*** list)
{
	int i = 0;
	while((*list)[i][0] != '\0')
		i++;
	return i;
}